{
  "name": "webserver",
  "version": "1.0.0",
  "description": "web server for pi",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pedro",
  "license": "MIT",
  "dependencies": {
    "onoff": "^6.0.3",
    "socket.io": "^4.4.0"
  }
}
